## Environment variables for backend (copy to .env and fill values)
# Do NOT commit your real .env to git. This file is an example only.

# MongoDB connection string (Atlas or local)
MONGO_URI=mongodb://127.0.0.1:27017/intervuiq_dev

# JWT secret used to sign tokens (change to a strong value in production)
JWT_SECRET=your_jwt_secret_here

# Frontend origin(s) allowed by CORS (comma-separated). Example includes Vite (5173) and Vite preview/other (8080)
CLIENT_ORIGIN=http://localhost:5173,http://localhost:8080

# Server port (optional, defaults to 8000)
PORT=8000

# NODE environment
NODE_ENV=development

# Optional: OpenAI API key for resume analysis (set this in your real .env, do NOT commit it)
# Example: OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# If you accidentally pasted a real key into a public place, rotate/revoke it immediately.
OPENAI_API_KEY=
